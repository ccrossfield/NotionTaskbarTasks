# Claude Code machine-local state (settings, worktrees, installed skills).
# Installed skills are third-party deps from mattpocock/skills; the committed
# skills-lock.json is the source of truth. Restore on a fresh clone with:
# npx skills experimental_install
.claude/
.agents/skills/

# Swift Package Manager build output + assembled app bundle
.build/
NotionTasks.app/

# macOS
.DS_Store

# Throwaway API spike (deleted; ADR-0001 records its findings). Ignored so a
# recreated scratch spike never lands in a commit by accident.
spike/
